import styles from "./constructionProgress.module.scss"

const ConstructionProgress = () => {
  return <div className={styles.constructionProgress}>ConstructionProgress</div>
}

export default ConstructionProgress
